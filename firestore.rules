rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /reservations/{id} { allow read, write: if false; }
    match /checkinSessions/{sessionId} {
      allow get: if true; allow list: if false; allow create, update, delete: if false;
    }
  }
}
